# 06｜面试讲述与高频追问

## 一、30 秒版本

拾寻不是一个单点识图 Demo，而是失物场景的 AI 协同闭环。我把 AI 放在两个最合适的位置：一是把口语描述结构化，降低发布成本；二是从图片提取可比较语义。最终匹配由类别、地点、时间、文本、图像等 7 维规则排序，并展示证据覆盖和缺失项。原型还串了隐私模糊、模拟实名、认领审核、通知和双边归还。当前没有真实运营数据，所以我同时设计了 Top-K、误匹配率、人工确认率和全漏斗指标，明确下一步如何验证。

## 二、3 分钟版本

### 1. 问题

失物招领的难点不是“有没有信息”，而是信息散落、描述不统一、发布时间不同，而且联系方式和具体地点存在隐私风险。用户真正要完成的是从发布到安全交接，而不是得到一个模型标签。

### 2. 产品定位

我把项目定位成“AI 失物协同闭环”：用 AI 降低信息录入成本和补充语义证据，用可解释匹配降低搜索成本，用权限和认领审核降低误领与隐私风险。

### 3. AI 设计

自然语言结构化接 SiliconFlow 文本模型；图片接 Qwen-VL。模型失败时不能让主流程瘫痪，所以文本回退启发式规则，图片回退浏览器端直方图、感知哈希与主色。页面会显示实际来源，不把兜底结果伪装成 AI。

### 4. 匹配设计

候选排序综合类别、颜色、地点、时间、文本、图像和语义 7 维。原版缺图时给固定分，容易虚高；我改成缺失维度剔除、权重重分配，再用证据覆盖系数保守降分。页面既显示分数，也显示匹配理由和缺失字段。分数是排序分，不是准确率。

### 5. 闭环与安全

寻物帖在模拟实名后可联系；招领帖必须模拟实名、回答验证问题并经发布者审核才解锁。之后通过通知、取件码和双边确认完成归还。微信登录和实名目前是 Mock，机构和商业化也是方案，页面和文档均有标注。

### 6. 验证与商业化

离线评测看 HitRate@1/3/5、MRR、通知阈值误匹配率；线上看发布完成、人工修正、通知打开、认领审核和完成率。商业上先验证高校、园区、商场的机构工作台与代保管场景，再判断 SaaS、按件服务或 API 是否成立，不提前写收入结果。

## 三、STAR 讲法

- S：失物信息非结构化、供需错位，直接公开详细信息又有隐私和误领风险。
- T：把已有原型升级成能说明 AI 价值、产品闭环、评测与商业边界的求职项目。
- A：审计真实代码；明确 AI / 规则 / Mock；补离线演示；重构缺失感知匹配；设计审核解锁；补 Evaluation、埋点和商业验证方案。
- R：产出可运行副本、完整项目文档和展示脚本。结果是“原型与验证框架完成”，不是虚构的线上增长。

## 四、高频追问

### Q1：为什么这个场景需要 AI？规则不够吗？

规则适合类别、时间和地点等明确字段，但用户输入常是口语、图片和长描述。AI 的价值是把非结构化输入变成字段和语义标签；权限、阈值和业务状态仍由确定性规则控制。这样既利用模型泛化，也保留可解释和可兜底。

### Q2：你的“AI 匹配”到底是不是模型？

不是端到端匹配模型。当前是规则加权排序，AI 提供文本结构化和图像语义两个输入维度。面试时我会称它“融合 AI 语义的多维可解释匹配”，不会称为训练出的匹配模型。

### Q3：为什么不用向量数据库？

原型数据量小，先用可解释基线验证字段和流程更划算。若标注集证明文本同义表达是主要瓶颈，再引入 embedding + ANN，并保留类别 / 时间硬过滤、重排和解释层。技术升级应由误差分析驱动。

### Q4：权重怎么来的？

是产品原型初始权重，不是训练或线上实验结果。类别、地点、时间体现事件一致性，图像和语义占比较高用于差异化。下一步用标注集做消融与网格 / 学习排序校准，并按品类、有无图片分群。

### Q5：80 分为什么叫高匹配？

80 是演示阈值，只用于页面排序标签；75 是演示通知阈值。正式上线要用 PR 曲线、FalseMatchRate@T、通知后确认率和投诉率决定，而且高风险品类要更严格。

### Q6：如何评价效果？

离线看 HitRate@1/3/5、MRR、nDCG 和阈值误匹配率；切片看有图/无图、字段缺失、品类和城市。线上用候选确认、通知打开、认领提交和双边归还作为分层代理指标，不能只拿点击率当找回率。

### Q7：没有真实数据，项目价值怎么证明？

目前证明的是：问题拆解、可运行实现、异常兜底、指标口径和验证路径完整。不能证明真实市场效果。下一步先做 200～500 组标注对和 1 个校园 / 园区小试点，再基于数据决定权重和商业模式。

### Q8：模型幻觉怎么处理？

模型输出必须 JSON 化、字段白名单和长度限制；前端展示置信度并允许人工修改；模型失败回退规则；匹配只把语义当一个维度；最终认领需要验证问题和人工审核。高风险字段不由模型自动解锁。

### Q9：图片识别失败怎么办？

仍保留本地图像直方图、感知哈希、颜色和用户手填字段。若连 Canvas 也失败，就把图像维度标记缺失并重分配权重，而不是补一个默认分。页面提示用户换图或继续手填。

### Q10：隐私机制是否真的安全？

服务端已对地点、时间、描述和联系方式脱敏，招领信息需实名状态和审核通过后解锁；但实名是 Mock，Storage 还是公开 URL，所以只能称原型。生产版要接权威认证、私有 bucket、签名 URL、访问审计、数据过期删除和申诉机制。

### Q11：Supabase Service Role 放在服务端就安全吗？

比放前端安全，但仍需最小权限、密钥轮换和环境隔离。当前 Functions 使用 Service Role，RLS 默认拒绝浏览器直连；生产化可进一步拆专用数据库角色、限制路由字段、加审计和 WAF。

### Q12：哪些功能是 Mock？

微信登录、实名校验、机构合作、代保管外部系统、系统级 Push；离线模式下认领 / 评价 / 举报也只做流程兜底。SiliconFlow 与 Supabase 是真实调用代码，但交付包无 Key，不能把未执行的在线调用说成已验证线上能力。

### Q13：商业化怎么做？

先做需求验证而不是直接收费。优先找失物量集中、有专人处理的高校、园区、商场，验证机构工作台能否降低登记、查询和交接成本。通过工单量、处理时长、闭环率和人力成本评估 SaaS；代保管按核销件数；API 适合已有服务台系统的客户。

### Q14：最大风险是什么？

第一，冷启动导致候选不足；第二，高分误匹配和恶意认领损伤信任；第三，图片、位置、联系方式的隐私合规。对应策略是机构种子供给、严格通知阈值 + 人工审核、私有存储 + 最小化采集。

### Q15：如果再给两周做什么？

- 第 1 周：制作标注集、建立 baseline / 消融、补服务端事件日志和私有图片方案。
- 第 2 周：做校园小试点、校准阈值、上线通知频控与审核后台，验证发布—认领漏斗。

## 五、反问面试官时可延展

- 团队如何定义 AI 功能的“可用”与“可商业化”？
- 模型效果、成本和业务转化由谁共同负责？
- 是否有离线标注平台、在线实验和人工审核基础设施？
- 对 AI PM 的期望更偏模型策略、增长闭环还是行业解决方案？