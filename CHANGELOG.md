# CHANGELOG

## 2026-07-15｜合并前本地整合与 QA

- 在本地分支 codex/merge-portfolio-fixes-20260715 整合求职优化版；原始 HEAD 由 codex/pre-merge-backup-20260715 和桌面快照双重保留。
- 保留 Supabase 在线记录、真实 image_data、Storage 上传、认领申请、通知和多账号审核闭环；未连接生产数据库。
- 清晰展示实物图片，隐私边界改为联系方式、街道、详细位置和认领答案；取消整图高斯模糊。
- 删除卡通图片种子引用和合成图片生成器；缺图或加载失败时显示中性文字占位。
- 修复中文静态图片 URL 解码、统计值 0 显示、匹配页图片失败兜底。
- 移除首页黄色作品集提示、卡片“演示数据”标签；Evaluation 仍保留指标真实性边界。
- 离线单账号认领明确返回未送达，不宣称真实通知成功。
- 离线 QA：项目检查通过，启发式回归 21/21，Smoke Test 24/24；未部署、未推送、未更新域名。

## 2026-07-10｜AI 产品求职优化版

### 定位与真实性

- 将项目定位统一为“AI 失物协同闭环”，弱化泛化的“AI 自动匹配”口号。
- 页面增加作品集演示状态条，区分内存示例与 Supabase 数据源。
- 明确登录 / 实名为 Mock、页面数字为演示数据、排序分不等于准确率。
- 机构、代保管与商业化内容改为“原型 / 产品方案”，不再暗示真实合作。

### 可运行性

- 未配置 Supabase 时，`records` 列表返回内存种子数据。
- 未配置 Supabase 时，发布记录写入进程内存，支持离线主流程演示。
- 增加 `.env.example`、`npm run check` 和 `scripts/verify-project.js`。
- 保留 Node 18+ 本地启动方式，不要求真实密钥。

### AI 与匹配

- 保留 SiliconFlow 文本模型、Qwen-VL 图像模型真实调用路径。
- 保留文本模型 → 降级模型 → 启发式规则的三级兜底。
- 标记图像语义来源为 `ai` 或 `heuristic`，避免把回退标签称为 AI 结果。
- 将 7 维匹配从“缺失图片给固定分”改为缺失感知。
- 新增动态权重重分配、证据覆盖、低覆盖惩罚和缺失字段理由。
- 匹配页面补齐语义维度，并将“强烈推荐”改为“高排序候选”。
- 匹配页按需提取双方本地图像特征；提取失败时将图像维度标记缺失。

### 隐私与安全

- 招领信息改为模拟实名 + 认领审核通过后解锁；寻物信息保留模拟实名门槛。
- Mock 实名弹窗提醒不要输入真实姓名或证件信息。
- 无显式 JWT Secret 时使用进程级随机密钥，移除固定公开 fallback secret。
- 管理员诊断和种子同步接口增加角色校验。
- 从 Vercel 构建与路由移除破坏性 `migrate-city` 接口。
- Supabase schema 默认拒绝 anon/authenticated 直接读写。
- 补齐 `shiyun_resolved_records` 与 `shiyun_reports` 表定义。

### 文档

- 重写作品集级 README。
- 新增项目资产、AI 匹配、Evaluation、架构、简历、面试、截图 7 份专项文档。
- 所有成果表述避免虚构用户、准确率、找回率、客户和收入。

### 仍未解决

- 微信 OAuth、权威实名、机构系统与系统级 Push 仍未接入。
- Supabase Storage 仍为公开 URL，生产化需私有 bucket + 签名 URL。
- 匹配权重和阈值尚未通过真实标注集校准。
- 离线认领 / 评价 / 举报仅用于演示，服务重启后不持久化。
## 2026-07-12 第二轮定向优化与验收

- 修复启发式字段串位、联系方式误判、地点边界和模糊时间伪造问题。
- 增加字段置信状态、人工确认、首页 loading/error/empty 三态和重试。
- 新增 12 条回归、24 项 Smoke Test、7 张真实截图及 verification 证据包。
- 保留真实 SiliconFlow/Supabase 接入和旧环境变量兼容。
- 移除副本中的 `vercel --prod` npm 快捷脚本；本轮未部署。

## 2026-07-13 合并前微修

- 移除 6 个未使用外部依赖并重建最小锁文件，解决无关 skia-canvas 原生下载风险。
- 新增 9 条非模板启发式测试；原 12 条与新增用例合计 21/21 通过。
- 仅改进通用边界与置信状态，未新增业务功能、未修改页面、未部署。
