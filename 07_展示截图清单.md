# 07｜展示截图清单

目标：用 8～12 张图讲清“问题—AI—匹配—隐私—闭环—评测”，每张图都注明数据与能力状态。不要截入真实姓名、电话、证件号、API Key 或 Supabase 后台密钥。

## 截图前准备

1. 运行 `npm start`，打开 `http://localhost:4173`。
2. 清理浏览器中该站点的旧 localStorage，保证看到新的演示数据源提示。
3. 使用示例昵称和 `0000`，不要填写真实身份信息。
4. 默认离线模式即可完成大部分截图；需要真实 AI 成功图时，只在本机环境变量中临时配置自己的 Key，截图后立即移除。
5. 浏览器宽度建议：桌面 1440×900，移动 390×844。

## 必选截图

### 01｜首页与真实性提示

- 页面：失物广场。
- 画面：侧边导航、示例卡片、顶部“作品集演示版”状态条。
- 标注：`数据源：内存示例（Mock，可离线运行）`；数字不代表真实运营。
- 讲述：先建立项目定位与真实性边界。

### 02｜自然语言自动填表

- 页面：智能发布。
- 输入：`昨天晚上在国贸地铁站捡到一个黑色耳机盒，交给服务台`。
- 离线截图：保留“AI 暂不可用，已使用本地提取”的提示。
- 在线截图（可选）：保留模型名与置信度提示。
- 讲述：同一交互支持真实模型和规则降级，结果可人工修改。

### 03｜图片识别成功与失败兜底

- 页面：智能发布，上传无敏感信息的物品图。
- 成功图：主色、感知哈希、Qwen-VL 语义、置信度。
- 兜底图：本地图像特征成功但“未获得语义识别结果”。
- 建议并排展示，标题写“真实视觉 API / 本地特征兜底”。

### 04｜7 维可解释候选

- 页面：多模态可解释匹配。
- 画面：Top 候选的圆环分、7 条维度、证据覆盖、匹配理由。
- 标注：排序分 ≠ 准确率；基础权重与字段缺失动态重分配。

### 05｜字段缺失与保守降分

- 选择缺图或缺时间的记录。
- 画面：维度显示“缺失”，理由显示“对应权重已剔除”，覆盖率降低。
- 讲述：这是针对默认分抬高结果的产品 / 算法修正。

### 06｜隐私模糊状态

- 未完成模拟实名。
- 画面：模糊图片、地点 / 时间 / 描述脱敏、联系方式锁定。
- 标注：寻物与招领采用不同解锁路径。

### 07｜Mock 实名提示

- 画面：实名认证弹窗。
- 必须保留“Mock”“请勿填写真实信息”。
- 输入仅用 `测试用户 / 0000`。
- 讲述：演示权限状态，不冒充权威认证接入。

### 08｜认领问答与审核解锁

- 画面 A：完成模拟实名后的验证问题与申请按钮。
- 画面 B：发布者消息中心的同意 / 拒绝操作。
- 画面 C（可选）：审核后联系方式解锁。
- 若离线模式无法持久化完整状态，截图标题写“流程 Mock”；使用 Supabase 完成时写“真实持久化路径”。

### 09｜通知与双边归还

- 页面：消息中心 / 详情。
- 画面：认领审核、拾到者确认归还、失主确认收到。
- 讲述：产品目标是闭环，不只做候选推荐。

### 10｜演示数据概览

- 页面：演示数据概览。
- 画面：记录数、今日记录、高排序候选、含语义字段及状态边界卡片。
- 必须保留“不代表真实用户规模或业务结果”的说明。

## 文档型截图

### 11｜系统架构图

- 来源：`04_产品架构与数据流程.md`。
- 画面：浏览器、Functions、SiliconFlow、Supabase、匹配与认领关系。
- 用途：面试解释“AI 在哪里、规则在哪里、数据在哪里”。

### 12｜Evaluation 框架

- 来源：`03_Evaluation与运营指标.md`。
- 画面：Top-K、误匹配率、人工确认率和发布 / 通知 / 认领漏斗。
- 用途：证明不是只做功能，没有编造成果。

## 移动端补充

- 首页卡片 + 底部 Tab。
- 智能发布表单。
- 匹配候选维度条。

移动端只选 1～2 张，不要重复桌面版全部内容。

## 每张图的统一角标

建议作品集中使用以下角标之一：

- `已实现代码`
- `真实 API 路径｜需自有 Key`
- `Mock 流程`
- `演示数据`
- `Evaluation 方案`
- `商业化假设｜未验证`

## 不要出现

- “准确率 95%”“找回率提升 XX%”等无数据结论。
- 把种子记录数量写成用户量。
- 把站内轮询写成微信 / 系统 Push。
- 把 Mock 实名写成公安 / 运营商认证。
- 把机构列表写成已签约合作方。
- 任何真实 Key、电话、身份证、Supabase URL 截图。
## 第二轮实际截图（2026-07-12）

实际文件位于 `docs/screenshots/`，验收表见 `docs/verification/screenshot-checklist.md`。7 张截图均由本地离线服务的真实页面生成，不包含静态假 UI；其中演示数据、Mock 或“方案/待采集”均在页面标识。
