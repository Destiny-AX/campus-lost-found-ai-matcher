# 01｜项目资产与完成度盘点

盘点日期：2026-07-10。结论基于交付副本中的真实代码，不把演示数据、页面文案或产品设想当作线上结果。

## 一、资产总览

| 资产 | 代码证据 | 状态 | 备注 |
|---|---|---|---|
| 前端单页应用 | `index.html`、`style.css`、`script.js` | 已实现 | 失物广场、智能发布、匹配、消息、我的、数据概览 6 个视图 |
| 本地 Node 服务 | `local-server.js` | 已实现 | 静态文件 + `/api/*` 路由，Node 18+ 可运行 |
| Vercel Functions | `api/*.js`、`vercel.json` | 已实现 | 部署路由包含 records、auth、structured-input、analyze-image、notify、custody、upload-image |
| Supabase 数据库 | `supabase-schema.sql`、`api/_shared.js` | 真实接入代码 | 通过服务端 REST 调用；交付包不含 URL/Key |
| Supabase Storage | `api/upload-image.js` | 真实接入代码 | 上传到 `lost-found-images`；未配置时返回 Base64 作为演示兜底 |
| SiliconFlow 图像识别 | `api/analyze-image.js` | 真实 API 代码 | 默认 `Qwen/Qwen3-VL-8B-Instruct`，交付环境未做带 Key 的在线验证 |
| 自然语言结构化 | `api/structured-input.js` | 真实 API + 本地兜底 | 默认 DeepSeek-V4-Flash，失败再试 Qwen3-8B，最终启发式提取 |
| 7 维匹配 | `script.js` | 已实现 | 类别、颜色、地点、时间、文本、图像、语义；规则加权，不是训练出的端到端模型 |
| 隐私模糊 | `api/records.js` | 已实现 | 地点、时间、描述、联系方式；发布者例外 |
| 认领审核 | `api/records.js` | Supabase 模式已实现 | 无 Supabase 时仅返回演示成功，不持久化完整审核状态 |
| 通知 | `api/notify.js`、`script.js` | 已实现 | Supabase 持久化或内存队列；前端 8 秒轮询，不是系统级 Push |
| 微信登录 | `api/auth.js` | Mock | 不调用微信 OAuth，以随机 mock openid 建账号 |
| 实名认证 | `api/auth.js` | Mock | 仅校验姓名长度与 4 位数字，并保存截断 hash |
| 代保管点 / 机构 | `api/custody.js`、前端示例 | 原型 + Mock 数据 | 未接便利店、丰巢、物业、地铁或公安真实系统 |
| 评价 / 举报 / 双边归还 | `api/records.js`、schema | Supabase 模式已实现 | 离线模式为流程演示；未做生产级风控 |

## 二、前端页面

1. 失物广场：类型、类别、地区筛选；卡片、详情、隐私模糊与状态展示。
2. 智能发布：自然语言自动填表、结构化表单、图片上传、保管状态和验证问题。
3. 多模态匹配：Top-5 候选、7 维分解、证据覆盖、理由与缺失提示。
4. 消息中心：匹配、认领、审核、归还、积分消息轮询。
5. 我的：演示账号、Mock 实名、信用、等级、徽章、我的发布。
6. 演示数据概览：当前数据集统计，页面明确“不代表真实运营结果”。

## 三、后端与数据

### 部署路由

- `GET/POST/DELETE/PATCH /api/records`
- `POST /api/structured-input`
- `POST /api/analyze-image`
- `/api/auth?action=...`
- `/api/notify?action=...`
- `/api/custody?action=...`
- `POST /api/upload-image`

`api/migrate-city.js` 保留为历史维护脚本，但已从 Vercel build 与 route 中移除；本地服务也不路由它。

### 数据表

优化后的 `supabase-schema.sql` 包含：

- `lost_found_records`
- `shiyun_users`
- `shiyun_custody_points`
- `shiyun_notifications`
- `shiyun_credit_logs`
- `shiyun_reviews`
- `shiyun_claim_requests`
- `shiyun_resolved_records`
- `shiyun_reports`

原 schema 缺少 `resolved_records` 与 `reports`，本次已补齐。RLS 默认拒绝 anon/authenticated 直接访问，服务端 Service Role 通过 Functions 读写。

## 四、AI 能力真实性

### 真实 API 调用代码

- 图片：向 SiliconFlow `/v1/chat/completions` 发送文本 + Base64 图片，要求 JSON 对象。
- 文本：向同一接口发送结构化抽取 prompt，主模型失败后切换降级模型。
- Supabase：REST CRUD 与 Storage 上传均是真实 HTTP 请求。

“有真实调用代码”不等于本交付环境完成在线调用。由于不携带密钥，本次运行验证只覆盖离线模式、鉴权、兜底和 API 返回结构。

### 已实现的本地能力

- 图片缩放、颜色直方图、主色、色板、感知哈希。
- 文本 token / Dice 相似度。
- 类别关联、地点邻接、时间差、颜色距离。
- 字段缺失检测、权重重分配、证据覆盖惩罚、理由生成。

### Mock / 示例

- 自动演示登录、微信登录、实名校验。
- 未配置 Supabase 时的内存种子记录与会话内发布。
- 机构、代保管点、公开机构标签与部分流程结果。
- 页面中的数量和分数是当前演示数据计算值，不是线上 KPI。

## 五、本次优化的完成度判断

### 可用于求职展示

- 完整主线与能力边界已统一。
- 离线可运行，避免面试现场因外部服务不可用而中断。
- 页面、README、简历、面试、评测与截图材料齐备。
- 评分从固定缺失分改为缺失感知，解释性更强。
- Mock、真实 API、产品方案、后续规划已有显式标签。

### 尚未达到生产标准

- 没有真实 OAuth / 实名服务。
- 没有带标注集的匹配效果报告。
- 没有真实消息 Push、内容审核、风控或客服后台。
- Storage 仍使用公开 URL，生产化应改私有 bucket + 签名 URL。
- 内存限流和内存数据不适合多实例部署。
- 未接真实机构与商业付费链路。

## 六、展示建议

把项目定义为“AI 失物协同闭环原型”，重点讲产品决策、模型调用边界、可解释评测与异常兜底；不要称为“已上线规模化平台”，不要把示例分数写成准确率，也不要把机构示例写成合作客户。