# 02｜AI 调用与匹配逻辑说明

## 一、AI 在产品中的实际作用

AI 不负责整个业务闭环，也不直接决定物品归属。它承担两类“非结构化信息转结构化证据”的工作：

1. 发布提效：把用户口语描述转换成表单字段，减少填写成本。
2. 图片理解：把图片转换成物品名、类别、颜色、品牌、可见文字、特征和置信度，补充传统字段。

最终候选排序由可解释规则引擎完成；用户仍需通过验证问题、认领审核和线下核验确认归属。

## 二、自然语言结构化

### 真实调用路径

`POST /api/structured-input`

请求：

```json
{ "text": "昨天晚上在国贸地铁站捡到一个黑色耳机盒，交给服务台" }
```

配置 `LOST_FOUND_SILICON_FLOW_API_KEY` 后，服务端调用 SiliconFlow Chat Completions：

- 主模型：`deepseek-ai/DeepSeek-V4-Flash`
- 降级模型：`Qwen/Qwen3-8B`
- 温度：0.1
- 输出约束：JSON object
- 超时：25 秒

目标字段包括 `type`、`title`、`category`、`color`、`location`、`district`、`street`、`detail_location`、`time`、`contact`、`item_status`、`description`、`confidence`。

### 异常兜底

| 异常 | 当前处理 | 前端提示 |
|---|---|---|
| 未配置 API Key | 直接本地启发式提取 | 明确“AI 暂不可用，已使用本地提取” |
| 主模型 HTTP / 超时失败 | 尝试 Qwen3-8B | 成功则标注实际模型 |
| 模型 JSON 解析失败 | 尝试下一模型 | 最终回退规则 |
| 所有模型失败 | `heuristic_fallback` | 提醒精度较低并人工修正 |
| 字段为空或异常 | `normalizeStructured` 清洗、分类纠正、时间解析 | 只填充有效字段 |

本地启发式规则可识别常见颜色、类别、相对时间、北京市区/街道、联系方式和保管状态。它是可用性兜底，不应称为 AI 模型结果。

## 三、图片语义识别

### 真实调用路径

`POST /api/analyze-image`

默认模型：`Qwen/Qwen3-VL-8B-Instruct`。请求采用多模态 message：文本 prompt + Base64 `image_url`，要求输出：

```json
{
  "object_name": "无线耳机盒",
  "category": "电子设备",
  "colors": ["黑色"],
  "brand_guess": "未知",
  "visible_text": [],
  "features": ["盒盖有划痕"],
  "confidence": 0.82
}
```

接口要求已登录，防止匿名消耗付费调用；超时 30 秒；支持从 Qwen reasoning content 中提取 JSON。

### 本地图像特征

无论视觉模型是否可用，浏览器都会尝试提取：

- 32×32 采样；
- 64 桶 RGB 颜色直方图；
- 主色与 6 色色板；
- 灰度均值感知哈希。

这部分是真实本地算法，不是模型 API。视觉模型失败时，页面明确显示“未获得语义识别结果，将使用本地图像特征匹配”。

### 图片异常

- 非图片文件：拒绝。
- 解析 / Canvas 失败：清空特征，允许用户换图。
- 无图片：生成明确的合成占位图用于演示；不把它当真实上传图片。
- Storage 未配置：保留 Base64，会话可用但不适合生产。
- Storage 上传失败：继续使用 Base64，不阻断发布。
- 视觉 API 未配置 / 超时 / 非 JSON：语义为空，匹配不计算真实 AI 语义证据。

## 四、7 维匹配

### 基础权重

| 维度 | 权重 | 计算方式 |
|---|---:|---|
| 类别 | 13% | 相同、预设关联、其他 |
| 颜色 | 8% | 命名颜色映射到 RGB 后计算颜色距离 |
| 地点 | 14% | 标准化地标、前缀与邻接关系 |
| 时间 | 11% | 0.5h / 2h / 8h / 24h 分段衰减 |
| 文本 | 14% | token Jaccard 与 Dice 系数组合 |
| 图像 | 20% | 直方图余弦 + 感知哈希 + 主色距离 |
| 语义 | 20% | 物品名、类别、颜色、特征/可见文字、品牌与置信度 |

### 缺失字段处理

原版对缺失图片返回固定 0.25，可能抬高无图候选。本次改为：

1. 判断每个维度是否存在双方可比较证据；
2. 缺失维度记为 `null`，不显示虚构百分比；
3. 将剩余维度权重按比例重分配；
4. 计算“证据覆盖率”＝可用维度基础权重之和；
5. 最终排序分乘以 `0.72 + 0.28 × 证据覆盖率`，证据少时保守降分。

公式：

```text
Weighted = Σ(score_i × weight_i) / Σ(available_weight_i)
Final = Weighted × (0.72 + 0.28 × Coverage)
```

这里的 Coverage 使用 0～1。该机制是产品原型规则，需通过标注集做阈值与权重校准。

### 可解释理由

页面最多展示 3 条正向证据，再补充缺失 / 低覆盖提醒，例如：

- 物品类别一致，是强匹配因素；
- 地点高度接近；
- 描述关键词重合：耳机、黑色；
- 缺失图像、时间，对应权重已剔除；
- 证据覆盖较低，建议补充字段后人工核验。

“AI 图像语义高度相似”只在双方语义来源均标记为 `ai` 时使用；种子标签或本地回退只称为“结构化语义标签”。

## 五、排序与业务阈值

当前页面取 Top-5，80 分以上显示“高排序候选”，75 分以上可触发演示匹配通知。阈值来自原型经验，不是线上实验结果。生产化时应分别校准：

- 候选展示阈值：偏召回；
- 主动通知阈值：平衡打扰与召回；
- 自动置顶阈值：偏精度；
- 高风险类别（证件、贵重物品）：要求更多验证证据。

## 六、已知限制

1. 地点邻接是手工词表，不是地图距离服务。
2. 文本相似未使用向量数据库，长文本与同义表达能力有限。
3. 图像算法对背景、角度、光照敏感。
4. 视觉语义 prompt 未用真实标注集系统评测。
5. 权重未按城市、品类、是否有图做分群校准。
6. Top-K 结果目前由前端计算，大数据量下应迁移到服务端或向量检索层。