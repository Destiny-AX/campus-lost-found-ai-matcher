# 08｜运行验证记录

验证日期：2026-07-11

## 环境

- Windows / PowerShell
- Node.js `v24.16.0`（项目要求 Node 18+）
- 验证模式：显式屏蔽 Supabase 与 SiliconFlow 环境变量的离线演示模式

## 自动检查

执行：

```bash
npm run check
```

结果：通过。

- 15 个必需文件存在；
- 13 个 JavaScript 文件完成语法解析；
- `vercel.json` 可解析且未暴露 `/api/migrate-city`；
- 未发现常见 OpenAI 兼容 Key、JWT、硬编码 Supabase Key 模式。

## HTTP 与接口

| 检查 | 结果 |
|---|---|
| `GET /` | 200 |
| 页面包含“作品集演示版” | 是 |
| `GET /api/records` | 200，`source=demo_memory` |
| 默认示例记录 | 40 条（仅用于演示，不是用户数据） |
| 未实名招领记录脱敏 | 20 / 20 |
| `POST /api/structured-input`（无 Key） | 200，`source=heuristic` |
| 离线发布持久化 | 会话内成功，`source=demo_memory` |
| 普通用户访问 `action=diag` | 403 |
| `GET /api/migrate-city` | 404 |
| Mock 实名 | 成功写入演示身份状态 |
| 实名后招领信息（未审核） | 仍保持脱敏 |
| 离线认领 | 返回 `fallback=true`，仅流程演示 |

## 页面验证

使用 Microsoft Edge headless 以 1440×1000 打开本地页面并生成：

- `screenshots/页面验证_桌面端.png`

浏览器成功输出 178,486 字节截图；控制台只出现 Edge headless 自身的窗口 / 网络同步提示，未在命令输出中发现项目 JavaScript 崩溃信息。

## 未验证项

因交付包不含真实密钥，本次没有执行：

- SiliconFlow 在线文本模型请求；
- Qwen-VL 在线图片请求；
- Supabase 真实数据库 CRUD；
- Supabase Storage 真实上传；
- Vercel / Render 线上部署。

这些能力已确认存在真实 HTTP 调用代码与环境变量入口，但不能把“代码存在”表述为“本次在线调用成功”。

## 敏感信息结论

- 交付目录只含空值 `.env.example`。
- 未复制源项目 `.vercel`、`.git`、`node_modules`、调试 token 和代理配置。
- 未把任何真实 API Key 写入文档、前端或压缩包。
## 第二轮验收（2026-07-12）

- `npm install`：正式工作副本复跑成功；临时镜像首次因网络重置失败，失败日志亦保留。
- `npm run check`：通过，19 个必需文件、15 个 JavaScript 文件，未发现常见硬编码密钥。
- `npm run test:heuristic`：12/12 通过。
- `npm run test:smoke`：24/24 通过，使用随机端口并验证释放。
- `npm start`：在 4187 启动成功，实际用于 API 取证和 7 张截图。
- 在线模型、Supabase、Vercel Preview：仅代码检查或未验证，本轮未调用/连接/部署。
