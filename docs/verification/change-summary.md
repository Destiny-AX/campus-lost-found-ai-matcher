# 第二轮变更摘要

- 重写离线自然语言抽取：严格联系方式、地点边界、显式时区、原始时间表达、字段状态与人工确认。
- 增加 12 条启发式回归测试并保留 JSON/Markdown/控制台输出。
- 首页列表使用真实 `/api/records` 响应；区分 loading/error/empty；增加超时、错误提示、重试与 demo 标识。
- 发布后更新实际记录与统计，不以静态 40 掩盖异常。
- 补充字段置信面板、发布前确认、离线/在线来源提示。
- 保留 SiliconFlow 文本/视觉 HTTP 与 Supabase REST/Storage；强化超时和降级边界。
- 匹配展示保留 7 维、缺失维度、动态重分配、证据覆盖与理由。
- 增加 Evaluation/运营漏斗方案页，明确“方案/待采集”。
- 新增 24 项 Smoke Test、可控随机端口、关闭与端口释放验证。
- 生成 7 张真实本地运行截图、验收证据和关键 API 响应。
- 移除本副本 `vercel --prod` npm 快捷脚本；未运行部署。
- 未删除业务文件；最终包排除 `.git`、`.vercel`、`.env`、`node_modules` 与浏览器数据。
